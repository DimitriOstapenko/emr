<% provide(:title, 'New Paystub') %>
<% provide(:button_text, "Generate Paystub") %>

<div class="col-sm-8 col-sm-offset-2">
<h3 class='display-1'>Create <%= Time.now.strftime("%B") %> Paystub </h3>
    <%= render 'form' %>
</div>
