<% provide(:title, 'New Visit') %>
<% provide(:button_text, 'Save') %>
<h1>New Visit - <%= Patient.find(params[:patient_id]).full_name %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
