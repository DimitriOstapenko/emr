<% provide(:title, 'Welcome') %>
<% hcv = SvcMonitor.find_by(name: 'hcv')  %>

<font size=3>
  <div class="hdr-text"><center> <i class='glyphicon glyphicon-time'></i> 24/7 Online Visits</center></div>
  
  <% if current_patient && current_patient.user && current_patient.user.confirmed_at %>

  <h2> Welcome back, <%= current_patient.fname %> </h2>
  <%= render "/devise/sessions/new" %>
  <% else %>

  <div class="row pt-3">
  <div class="col-md-4 col-md-offset-4">
    <ul>
      <li> 100% OHIP covered </li>
      <li> Fast doctor callbacks </li>
      <li> Prescription renewals over the phone </li>
    </ul>
  </div>
  </div>
  <%= render "#{device_type}_lookup" if hcv.up %>
  <div class="pt-2"><center> Already registered? Please log in below. </center></div>

  <div align="center" class="pt-4">
  <%= link_to 'Request Doctor Callback', new_user_session_path, class: "btn btn-success btn-lg" %></div>
  <% end %>


  <div class="ml-3 pt-2">
  <%= Schedule.doc_on_duty %>
  </div>

    <div class="pt-3"></div>
    <div class="panel panel-info news-panel-body">
      <div class="panel-body "><p align="justify"> 
       Our goal is to call you back within an hour of your request, if made between 8am and 8pm. We can't guarantee prompt callbacks during off hours.
  <%= render "schedules/full_schedule" %>
    </div>
  </div>
</font>

<div class="pt-3"> <%= link_to 'Other clinics in Stoney Creek', other_clinics_path %> </div>

<div class='pt-5'>
<div class='pt-5'>
<%= render "footer_icons" %>
</div>
